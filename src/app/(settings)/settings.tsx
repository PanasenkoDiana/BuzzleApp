import { ScrollView, View } from "react-native";
import { Settings } from "../../modules/settings/pages/settings-page";


export default function SettingsPage(){

    return(
        // <ScrollView contentContainerStyle={{flex: 1}}>
        <Settings />
        // </ScrollView>
    )
}