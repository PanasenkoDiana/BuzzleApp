import { View, Text, TouchableOpacity, FlatList, Image } from "react-native";
import { IContact } from "../../types/types"

export function ContactsPage(props: IContact) {
    return (
        <View style={styles.container}>
            <Image source={}>
        </View>
    )
}